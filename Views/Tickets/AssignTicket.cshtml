@model Unbugit.Models.ViewModels.AssignDeveloperViewModel
@using Unbugit.Services.Interfaces
@using Microsoft.AspNetCore.Mvc.Rendering

@inject IBTCompanyInfoService CompanyInfoService;

@{
    int companyId = Model.Ticket.Project.CompanyId;
    List<BTUser> devs = await CompanyInfoService.GetMembersInRoleAsync("Developer", companyId);
}

@*<div id="AddNewTicketModal" class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content" style="border: 1px solid #DD44A7">
        <div class="modal-body text-center">*@

            <div class="row">
                <div class="col-lg-4 offset-lg-4 col-md-12">
                    <div class="card shadow" style="border: 1px solid #DD44A7">
                        <div class="card-body justify-content-center">
                            Assign a Developer for Ticket:<br />
                            <h5>@Model.Ticket.Title</h5>
                            <form asp-action="AssignTicket" method="post">
                                <input asp-for="@Model.Ticket.Id" hidden />
                                <input asp-for="@Model.Ticket.ProjectId" hidden />
                                <input type="hidden" name="returnUrl" value="@ViewBag.returnUrl" />

                                <div class="row clearfix">
                                    <div class="col-md-6 col-sm-12">
                                        <label>Select Developer </label>
                                        <div class="form-group">
                                            <select class="form-control" asp-for="DeveloperId" asp-items="Model.Developers">
                                                <option>Select Developer</option>
                                            </select>
                                        </div>
                                        <div class="mt-4 form-group">
                                            <button type="submit" class="btn btn-outline-primary">Submit</button>
                                            <button asp-action="Details" asp-controller="Tickets" asp-route-id="@Model.Ticket.Id" class="btn btn-outline-danger">Cancel</button>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>

        @*</div>
    </div>
</div>*@

